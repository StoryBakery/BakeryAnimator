---
title: Channel
---

`Section` 내부에서 실제 키프레임 곡선을 가지는 단위입니다.
스칼라 분해 채널(`X`, `Y`, `Z`)을 표현합니다.

## Attributes

### ChannelKey

`string`

채널 키입니다. (예: `"X"`, `"Y"`, `"Z"`, `"W"`)

### ValueType

`string`

채널 값 타입입니다. (예: `number`, `boolean`)
